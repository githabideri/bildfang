# video/

`camera.mp4` is **not committed** to the repository (it is real sensor data
and several GB in a real capture). In actual captures this directory also
contains `frames.json` (Phase 2: presentation-timestamp → ARCore frame
index mapping).
